<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.mentalmathbattle">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">

        <activity
            android:name=".ui.SplashActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.LoginActivity" />

        <activity
            android:name=".MainActivity" />

        <activity
            android:name=".ui.MatchActivity" />

        <activity
            android:name=".ui.BattleActivity" />

        <activity
            android:name=".ui.RegisterActivity" />

        <activity
            android:name=".ui.ResultActivity" />

        <activity
            android:name=".ui.TrainingActivity" />

        <activity
            android:name=".ui.LeaderboardActivity"
            tools:ignore="Instantiatable" />

        <activity
            android:name=".ui.ErrorBookActivity"
            tools:ignore="Instantiatable" />
        <activity
            android:name=".ui.TrainingQuestionActivity"
             />
        <activity
            android:name=".ui.TrainingHistoryActivity" />
        <activity
            android:name=".ui.TrainingDetailActivity" />
        <activity
            android:name=".ui.EmailBindActivity" />
        <activity
            android:name=".ui.ForgotPasswordActivity" />
        <activity
            android:name=".ui.ProfileSettingsActivity" />

    </application>

</manifest>